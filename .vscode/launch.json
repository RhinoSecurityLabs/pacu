{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Pacu",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/.venv/bin/pacu",
            "cwd": "${workspaceFolder}",
            "justMyCode": true,
            "console": "integratedTerminal"
        }
    ]
}